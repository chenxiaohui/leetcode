/*
 *  find a position where elements before(include itself) <= value and elements after >= value
 */
#include "common.h"

class Solution {
public:
  //size_t partition(vector<int> & input, int value) {
    //size_t ret = 0;
    //if (input.size() > 0) {
      //size_t i = 0;
      //size_t j = input.size() - 1;
    //}
    //return j;
  //}
};
Solution s;

