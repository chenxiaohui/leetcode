/*
Given two numbers represented as strings, return multiplication of the numbers as a string.Note: The numbers can be arbitrarily large and are non-negative.
*/

#include "common.h"

class Solution {
public:

};
Solution s;

TEST(MultiplyStrings, normal) {
}
